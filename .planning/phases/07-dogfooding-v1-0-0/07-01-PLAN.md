---
phase: 07-dogfooding-v1-0-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DOGFOOD.md
autonomous: true
requirements:
  - VALD-01
  - VALD-02

must_haves:
  truths:
    - "All tests pass cleanly (go test ./... exits 0)"
    - "Binary builds and responds to MCP initialize in <20ms"
    - "Both Phase 6 deferred fixes are confirmed applied in source"
    - "DOGFOOD.md template exists with daily-log structure"
  artifacts:
    - path: "docs/DOGFOOD.md"
      provides: "Daily dogfood log template"
      contains: "## Log"
  key_links:
    - from: "internal/registry/npm/npm.go"
      to: "io.LimitReader"
      via: "wrapping resp.Body before parsePackument"
      pattern: "LimitReader"
    - from: "internal/filter/nearest.go"
      to: "maxAlternatives cap"
      via: "if len(result) > maxAlternatives guard"
      pattern: "maxAlternatives"
---

<objective>
Confirm the pre-flight conditions are met before any dogfood work starts: both Phase 6 deferred code fixes are in place, the full test suite is green, the binary smoke-tests clean over stdio, and the DOGFOOD.md daily-log template exists.

Purpose: Gate on correctness before wiring the server into live agents. Nothing in Wave 2+ should start with a broken or unverified binary.
Output: Verified green test suite, confirmed fixes in source, `docs/DOGFOOD.md` template committed.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-dogfooding-v1-0-0/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: confirm Phase 6 deferred fixes are applied</name>
  <files>internal/registry/npm/npm.go, internal/filter/nearest.go</files>
  <read_first>
    - internal/registry/npm/npm.go (confirm LimitReader wraps resp.Body before parsePackument)
    - internal/filter/nearest.go (confirm doc-comment says "at most 5" AND the cap guard exists)
  </read_first>
  <action>
    Read both files and confirm:

    **Fix 1 - npm.go LimitReader (CONTEXT.md §C, Fix 1):**
    Search for `io.LimitReader` around the `parsePackument` call. The line must read:
    `p, err := parsePackument(io.LimitReader(resp.Body, maxPackumentBytes))`
    with `const maxPackumentBytes = 32 << 20` (or equivalent ≥16 MiB constant) declared nearby.
    If it is NOT present: add `const maxPackumentBytes = 32 << 20` above the call and wrap:
    `p, err := parsePackument(io.LimitReader(resp.Body, maxPackumentBytes))`

    **Fix 2 - nearest.go doc-comment and cap (CONTEXT.md §C, Fix 2):**
    Search for the `NearestVersions` doc-comment. It must say "at most 5" (not "at most 3").
    Search for `maxAlternatives` — the guard `if len(result) > maxAlternatives { result = result[:maxAlternatives] }` must exist.
    If the doc-comment says "at most 3" and/or the guard is missing: update the comment to "at most 5" and add the guard after all result entries are appended.

    If both fixes are already present (likely from Phase 6), make no changes — just confirm by reading.
  </action>
  <verify>
    <automated>grep -n "LimitReader" internal/registry/npm/npm.go && grep -n "maxAlternatives" internal/filter/nearest.go</automated>
  </verify>
  <done>npm.go shows `LimitReader` on the parsePackument call; nearest.go shows `maxAlternatives` constant and cap guard.</done>
</task>

<task type="auto">
  <name>task 2: run full test suite and binary smoke test</name>
  <files>docs/DOGFOOD.md</files>
  <read_first>
    - go.mod (confirm module path for build command)
  </read_first>
  <action>
    **Step 1 - Run full test suite:**
    ```
    go test ./...
    ```
    All packages must exit 0. If any package fails, fix the failure before proceeding.

    **Step 2 - Build binary:**
    ```
    go build -o /tmp/version-check-mcp-smoke ./cmd/version-check-mcp
    ```

    **Step 3 - Stdio smoke test (MCP initialize):**
    Send a minimal JSON-RPC initialize request over stdin and confirm a valid response on stdout with zero stray bytes:
    ```
    echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"smoke","version":"0.0.1"}}}' \
      | /tmp/version-check-mcp-smoke 2>/dev/null
    ```
    The output must be a single JSON line starting with `{"jsonrpc":"2.0"` containing `"result"` with `"protocolVersion"` and `"serverInfo"`. Any non-JSON byte on stdout is a failure.

    **Step 4 - Create docs/DOGFOOD.md:**
    Create the file with this exact content (adapt dates as needed):

    ```markdown
    # Dogfood Log — version-check-mcp v1.0.0

    **Window start:** <!-- fill in date when 07-03 is complete -->
    **Window target:** ≥7 days of daily active use
    **Agents under test:** Claude Code CLI (primary), OpenCode (secondary)
    **Binary:** built from main at <!-- fill in commit SHA -->

    ---

    ## Tracking

    ### P0 Bug Tracker

    | # | Description | Status | Opened | Closed |
    |---|-------------|--------|--------|--------|
    | — | *(none open)* | — | — | — |

    ### Observations

    Log notable events: rate-limit hits, not-found results, upstream-down events, unexpected outputs.

    | Date | Agent | Tool called | Input | Output | Notes |
    |------|-------|-------------|-------|--------|-------|

    ---

    ## Log

    <!-- One entry per day. Copy the template block below. -->

    ### Day N — YYYY-MM-DD

    **Agent used:** <!-- e.g. Claude Code CLI -->
    **Calls made:** <!-- e.g. 3x validate_version, 1x get_latest_version -->
    **Any issues:** <!-- P0/P1/none -->
    **Notes:** <!-- free text -->

    ---

    ## Sign-off

    - [ ] ≥7 days elapsed
    - [ ] Zero open P0 bugs
    - [ ] Rate-limit, not-found, and upstream-down events observed in stderr logs at least once each
    - [ ] Author approves v1.0.0 tag
    ```
  </action>
  <verify>
    <automated>go test ./... && test -f docs/DOGFOOD.md && grep -q "## Log" docs/DOGFOOD.md</automated>
  </verify>
  <done>go test ./... exits 0, binary smoke-test produces valid JSON-RPC response, docs/DOGFOOD.md exists with ## Log section and P0 tracker table.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → tool | DOGFOOD.md is local author-only; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | binary smoke output | accept | stdout-only check; author-controlled environment; no user data |
</threat_model>

<verification>
- `go test ./...` exits 0
- `grep LimitReader internal/registry/npm/npm.go` returns a match
- `grep maxAlternatives internal/filter/nearest.go` returns a match
- `docs/DOGFOOD.md` exists and contains `## Log` and the P0 tracker table
</verification>

<success_criteria>
All Phase 6 deferred fixes confirmed in source, full test suite green, binary responds correctly over stdio, DOGFOOD.md template committed and ready for daily entries.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dogfooding-v1-0-0/07-01-SUMMARY.md`
</output>
