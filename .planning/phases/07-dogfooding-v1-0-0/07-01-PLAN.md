---
phase: 07-dogfooding-v1-0-0
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/DOGFOOD.md
autonomous: true
requirements:
  - VALD-01
  - VALD-02

must_haves:
  truths:
    - "All tests pass with zero failures"
    - "Binary builds cleanly for darwin/arm64"
    - "Open STATE.md todos (MJ-01, MJ-03, MJ-04) are confirmed resolved or documented"
    - "DOGFOOD.md exists as a structured daily-use log template"
  artifacts:
    - path: "docs/DOGFOOD.md"
      provides: "Structured dogfood log with observation entries"
      contains: "## Dogfood Log"
  key_links:
    - from: "go test ./..."
      to: "all packages"
      via: "Go test runner"
      pattern: "ok.*github.com/JesperRossen"
---

<objective>
Pre-flight gate and dogfood scaffolding before wiring into Claude Desktop.

Purpose: Confirm the codebase is ship-ready (open code-review todos verified resolved, all tests green, binary builds) and create the DOGFOOD.md structured log the author will fill during the validation window.

Output: Verified clean test suite + DOGFOOD.md ready for entries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>task 1: verify open todos are resolved and all tests pass</name>
  <files>docs/DOGFOOD.md</files>
  <read_first>
    - .planning/STATE.md (open todos: MJ-01, MJ-03, MJ-04)
    - internal/mcp/tools_shape_test.go (MJ-01: check "package" vs "pkg" key)
    - internal/filter/nearest.go (MJ-03: latest_in_major candidate loop)
    - internal/registry/pypi/pypi.go (MJ-04: Versions() yanked filter)
  </read_first>
  <action>
1. Run the full test suite and capture output:
   ```
   go test ./... -count=1
   ```
   All packages must exit OK. If any fail, fix them before continuing.

2. Verify the three open todos from STATE.md:

   **MJ-01** (`tools_shape_test.go` sends `"package"` key but handler expects `"pkg"`):
   - Run: `grep -n '"package"\|"pkg"' internal/mcp/tools_shape_test.go`
   - Expected: only `"pkg"` appears (no bare `"package"` key in test inputs). If `"package"` is still present, update the test to use `"pkg"`.

   **MJ-03** (`latest_in_major` candidate loop skips `v == normLatest` too early):
   - Run: `grep -A5 "latest_in_major" internal/filter/nearest.go`
   - Expected: the loop does NOT skip `v == normLatest` during candidate collection (the fix uses `inResult` check at emit time only). The code comment "Do NOT skip normLatest here" confirms the fix is in place.

   **MJ-04** (PyPI `Versions()` includes yanked releases):
   - Run: `grep -A5 "func.*Versions" internal/registry/pypi/pypi.go`
   - Expected: `isYanked(files)` check filters yanked keys in the `Versions()` function body.

3. Build the binary for the local arch as a smoke test:
   ```
   go build -o /tmp/version-check-mcp-preflight ./cmd/version-check-mcp
   ```
   Binary must be produced without error.

4. Smoke test the binary responds to MCP initialize:
   ```
   echo '{"jsonrpc":"2.0","method":"initialize","id":1,"params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"test","version":"0"}}}' | /tmp/version-check-mcp-preflight 2>/dev/null
   ```
   Output must contain `"result"` and `"protocolVersion"` with no stray bytes before the JSON.

5. Create `docs/DOGFOOD.md` with the dogfood log template (see action below).
  </action>
  <action-dogfood-template>
Create `docs/DOGFOOD.md` with exactly this content:

```markdown
# Dogfood Log — version-check-mcp

**Start date:** <!-- fill when you install the MCPB -->
**Target window:** ≥7 days of daily use
**Status:** In progress

## Setup

- [ ] MCPB bundle installed into Claude Desktop
- [ ] Both tools (`validate_version`, `get_latest_version`) visible in tool list
- [ ] Structured logs confirm stderr output (not stdout)

## Daily Observations

> Use one entry per day. Record tool calls, outcomes, and any unexpected behavior.
> Capture log lines from stderr for rate-limited / not-found / upstream-down events.

### Day 1 — yyyy-mm-dd

**Tool calls made:** <!-- e.g. validate_version(npm, react, 18.3.1) -->
**Outcomes:** <!-- correct / incorrect / unexpected -->
**Logs (stderr):** <!-- paste relevant slog lines -->
**Issues found:** <!-- none / description -->

---

<!-- Copy the Day template above for each day -->

## P0 Bug Tracker

> P0 = stdout corruption, wrong-by-default latest-stable, scoped-package failure,
> +incompatible mishandling, Maven group-path bug.

| # | Bug | Reproduced? | Fixed? |
|---|-----|-------------|--------|
| - | -   | -           | -      |

## Verdict

**Window complete?** <!-- yes/no -->
**P0 bugs open?** <!-- none / list -->
**Ready to tag v1.0.0?** <!-- yes/no -->
```
  </action-dogfood-template>
  <verify>
    <automated>go test ./... -count=1 2>&1 | grep -v "^ok\|^?" | grep -c "FAIL" | grep -q "^0$" && echo "ALL_TESTS_PASS" || echo "TESTS_FAILED"</automated>
    <automated>test -f docs/DOGFOOD.md && grep -c "## Dogfood Log" docs/DOGFOOD.md | grep -q "^1$" && echo "DOGFOOD_MD_OK"</automated>
    <automated>go build -o /tmp/version-check-mcp-preflight ./cmd/version-check-mcp 2>&1 | grep -q "." || echo "BUILD_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./... -count=1` exits 0 with no FAIL lines
    - `grep -n '"package"' internal/mcp/tools_shape_test.go` returns empty (MJ-01 resolved)
    - `grep "Do NOT skip normLatest" internal/filter/nearest.go` returns 1 match (MJ-03 resolved)
    - `grep "isYanked(files)" internal/registry/pypi/pypi.go` returns >= 1 match inside Versions() (MJ-04 resolved)
    - `docs/DOGFOOD.md` exists and contains `## Dogfood Log`
    - Binary builds from `go build ./cmd/version-check-mcp` without error
    - MCP initialize smoke test returns JSON containing `"result"` on stdout
  </acceptance_criteria>
  <done>All tests green, all three STATE.md todos confirmed resolved, binary builds and responds to MCP initialize, DOGFOOD.md created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → binary | Smoke test runs the binary locally; no network calls in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | DOGFOOD.md | accept | Log file is local, author-only, no integrity requirements |
| T-07-02 | Repudiation | Preflight binary | accept | Smoke test output is observable stdout; no audit trail needed for local pre-flight |
</threat_model>

<verification>
- All Go tests pass: `go test ./... -count=1`
- MJ-01/MJ-03/MJ-04 todos confirmed resolved by grep checks
- `docs/DOGFOOD.md` exists with template
- Binary smoke test responds to MCP initialize with valid JSON-RPC response
</verification>

<success_criteria>
Pre-flight complete when:
- Zero test failures
- All three open todos from STATE.md verified resolved
- `docs/DOGFOOD.md` committed with daily-log template
- Binary builds and responds correctly to MCP initialize over stdin
</success_criteria>

<output>
After completion, create `.planning/phases/07-dogfooding-v1-0-0/07-01-SUMMARY.md`
</output>
