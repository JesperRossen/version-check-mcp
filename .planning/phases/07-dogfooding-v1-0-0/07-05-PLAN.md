---
phase: 07-dogfooding-v1-0-0
plan: 05
type: execute
wave: 5
depends_on:
  - 07-04
files_modified:
  - CHANGELOG.md
  - README.md
autonomous: false
requirements:
  - VALD-02

must_haves:
  truths:
    - "CHANGELOG.md exists with a ## [1.0.0] section listing all major features"
    - "README.md version badge / installation instructions reference v1.0.0"
    - "v1.0.0 git tag is pushed and GitHub Release is created as the first non-pre release"
    - "GoReleaser CI pipeline completes: 5 binaries + MCPB bundle attached to the release"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v1.0.0 release notes"
      contains: "## [1.0.0]"
  key_links:
    - from: "git tag v1.0.0"
      to: "GitHub Actions release.yml"
      via: "tag push triggers GoReleaser workflow"
      pattern: "v1.0.0"
---

<objective>
Write CHANGELOG.md, update README for v1.0.0, push the v1.0.0 git tag, and confirm the GitHub Release pipeline completes with all artifacts attached.

Purpose: Ship the first stable release. ROADMAP success criteria 6 requires a `v1.0.0` git tag as the first non-pre tagged release with a corresponding GitHub Release.
Output: CHANGELOG.md written, v1.0.0 tag pushed, GitHub Release live with 5 binaries + MCPB bundle.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-dogfooding-v1-0-0/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: write CHANGELOG.md and update README</name>
  <files>CHANGELOG.md, README.md</files>
  <read_first>
    - CHANGELOG.md (check if it already exists; if so, add a new section at the top)
    - README.md (find any version references or badges to update to v1.0.0)
    - .planning/ROADMAP.md (scan phase goals 1-7 for feature summaries to include in changelog)
  </read_first>
  <action>
    **Step 1 - Create/update CHANGELOG.md:**

    If CHANGELOG.md does not exist, create it. Add a `## [1.0.0] - YYYY-MM-DD` section at the top (use today's date) with the following content derived from the phase goals in ROADMAP.md:

    ```markdown
    # Changelog

    All notable changes to version-check-mcp are documented here.

    ## [1.0.0] - YYYY-MM-DD

    ### Added
    - MCP server over stdio: `validate_version` and `get_latest_version` tools with LLM-readable schemas
    - NPM registry adapter: full packument fetch, scoped packages (`@scope/pkg`), `dist-tags.latest`
    - PyPI adapter: PEP 440 normalization, yanked-release detection
    - Go Modules adapter: `+incompatible` suffix preservation, pseudo-version prerelease classification, `golang.org/x/mod` escape for capital letters in module paths
    - GitHub Actions adapter: `/tags` listing + `/releases/latest` hint, `rate_limited` on 403+`X-RateLimit-Remaining: 0`
    - Maven Central adapter: `group:artifact` parsing, group-path URL encoding, SNAPSHOT filter
    - Alternatives on miss: 3–5 ranked alternatives (`latest_stable`, `nearest_semver`, `latest_in_major`) on every `validate_version` miss
    - In-memory LRU+TTL cache (hashicorp/golang-lru/v2/expirable) with singleflight dedup
    - Multi-arch GoReleaser release: darwin/amd64, darwin/arm64, linux/amd64, linux/arm64, windows/amd64 — CGO_ENABLED=0 static binaries
    - MCPB bundle (`version-check-mcp.mcpb`) for one-click Claude Desktop install
    - Agent setup docs: Claude Desktop, Claude Code CLI, OpenCode (verified), Codex CLI, Gemini CLI (from official docs)
    - GITHUB_TOKEN env var guidance for 5000 req/hr GitHub API rate limit
    - Structured JSON logging to stderr (`log/slog`); stdout reserved for MCP protocol traffic only

    ### Fixed
    - `npm.go`: HTTP response body bounded with `io.LimitReader` (32 MiB) to prevent memory exhaustion on large packuments
    - `nearest.go`: `NearestVersions` result capped at 5 entries with explicit guard; doc-comment updated to match

    ### Notes
    - Go 1.25+ required (floor set by `github.com/modelcontextprotocol/go-sdk`)
    - Four direct dependencies: `modelcontextprotocol/go-sdk`, `hashicorp/golang-lru/v2`, `golang.org/x/sync`, `golang.org/x/mod`
    ```

    **Step 2 - Update README.md version references (if any):**
    Search README.md for any pre-release version strings like `v0.x.0` or `v1.0.0-rc` in badges, install commands, or download URLs. Update them to `v1.0.0`.
    If no version-specific strings exist (e.g., download links use "latest"), no changes needed.
  </action>
  <acceptance_criteria>
    - `test -f CHANGELOG.md` exits 0
    - `grep -c "## \[1.0.0\]" CHANGELOG.md` returns 1
    - `grep -c "validate_version" CHANGELOG.md` returns ≥1
    - `grep -c "LimitReader\|nearest.go" CHANGELOG.md` returns ≥1
  </acceptance_criteria>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c "## \[1.0.0\]" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md exists with a ## [1.0.0] section covering all major features and fixes from Phases 1-7. README updated if it contained version-specific strings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: push v1.0.0 tag and verify GitHub Release pipeline</name>
  <what-built>
    - CHANGELOG.md written with v1.0.0 release notes
    - README.md updated if needed
    - All prior plans complete: tests green, binary wired in Claude Code CLI, dogfood window signed off
  </what-built>
  <how-to-verify>
    1. **Final test run** before tagging:
       ```
       go test ./...
       ```
       All packages must pass. Do not tag on a red suite.

    2. **Commit CHANGELOG and any README changes:**
       ```
       git add CHANGELOG.md README.md docs/DOGFOOD.md .mcp.json
       git commit -m "chore: prepare v1.0.0 release"
       ```

    3. **Push the v1.0.0 tag:**
       ```
       git tag v1.0.0
       git push origin main
       git push origin v1.0.0
       ```

    4. **Watch GitHub Actions:**
       Open `https://github.com/JesperRossen/version-check-mcp/actions` and wait for the `release.yml` workflow triggered by the `v1.0.0` tag push to complete.

    5. **Verify GitHub Release artifacts:**
       Open the release page and confirm ALL of the following are attached:
       - `version-check-mcp_darwin_amd64` (or `.tar.gz`)
       - `version-check-mcp_darwin_arm64`
       - `version-check-mcp_linux_amd64`
       - `version-check-mcp_linux_arm64`
       - `version-check-mcp_windows_amd64` (or `.zip`)
       - `version-check-mcp.mcpb`
       - `checksums.txt` (SHA256)

    6. **Confirm it is NOT marked as pre-release** on the GitHub Release page (should show as "Latest release", not "Pre-release").

    **If the workflow fails:**
    - Check the Actions log for GoReleaser errors
    - Common issue: `GITHUB_TOKEN` not set in repo secrets — verify in Settings > Secrets
    - Common issue: tag already exists — delete and re-push if needed
    - Common issue: mcpb pack step fails — check `server/` directory layout matches manifest.json
  </how-to-verify>
  <resume-signal>
    Type "released" and paste the GitHub Release URL once all artifacts are confirmed. Or describe any pipeline failure.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git push → GitHub Actions | Tag push triggers release pipeline with GITHUB_TOKEN secret |
| GoReleaser → GitHub Release API | Uploads binaries using Actions token |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | release binaries | accept | GoReleaser produces SHA256 checksums; users can verify with `shasum -c checksums.txt` |
| T-07-05-02 | Repudiation | v1.0.0 tag | accept | Signed tags could be added post-v1; for now git history + GitHub Release provide sufficient trail |
| T-07-05-03 | Elevation of Privilege | GITHUB_TOKEN in Actions | accept | Token scoped to repo; standard GoReleaser pattern; not exposed in logs |
</threat_model>

<verification>
- `CHANGELOG.md` exists: `test -f CHANGELOG.md`
- `grep -c "## \[1.0.0\]" CHANGELOG.md` returns 1
- `git tag -l v1.0.0` returns `v1.0.0`
- GitHub Release at `https://github.com/JesperRossen/version-check-mcp/releases/tag/v1.0.0` shows 7 assets (5 binaries + .mcpb + checksums) and is NOT marked pre-release
</verification>

<success_criteria>
CHANGELOG.md written. v1.0.0 tag pushed. GitHub Release created as first non-pre release with 5 static binaries, MCPB bundle, and checksums all attached. Phase 7 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dogfooding-v1-0-0/07-05-SUMMARY.md`
</output>
