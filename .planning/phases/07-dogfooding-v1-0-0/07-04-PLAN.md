---
phase: 07-dogfooding-v1-0-0
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - docs/DOGFOOD.md
autonomous: false
requirements:
  - VALD-01
  - VALD-02

must_haves:
  truths:
    - "DOGFOOD.md has ≥7 daily log entries"
    - "P0 bug tracker shows zero open P0 bugs"
    - "At least one rate-limit, not-found, or upstream-down event was logged"
    - "Author has signed off on the window"
  artifacts:
    - path: "docs/DOGFOOD.md"
      provides: "Completed dogfood log with daily entries and sign-off"
      contains: "## Sign-off"
  key_links:
    - from: "docs/DOGFOOD.md"
      to: "Sign-off checklist"
      via: "all four checkboxes checked"
      pattern: "\\[x\\]"
---

<objective>
Run a ≥7-day dogfood window using the binary through Claude Code CLI (and optionally OpenCode), log daily observations in DOGFOOD.md, confirm zero open P0 bugs, and produce the author sign-off that gates the v1.0.0 tag.

Purpose: Real-world validation that the server behaves correctly under daily use — not just in tests. ROADMAP success criteria 4 and 5 require the window to elapse with observable log events and a clean P0 tracker.
Output: DOGFOOD.md with completed log and signed sign-off checklist.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/DOGFOOD.md
@.planning/phases/07-dogfooding-v1-0-0/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>task 1: fill DOGFOOD.md header and start date</name>
  <files>docs/DOGFOOD.md</files>
  <read_first>
    - docs/DOGFOOD.md (read current state; fill in window start date and binary commit SHA)
  </read_first>
  <action>
    Fill in the two placeholder values in the DOGFOOD.md header that were left blank in 07-01:

    1. **Window start** — replace `<!-- fill in date when 07-03 is complete -->` with today's date (YYYY-MM-DD format).
    2. **Binary commit SHA** — replace `<!-- fill in commit SHA -->` with the output of `git rev-parse --short HEAD`.

    Do not add log entries yet — those come during the window (see checkpoint task below).
  </action>
  <acceptance_criteria>
    - `grep -c "fill in" docs/DOGFOOD.md` returns 0 (no unfilled placeholders remain)
    - `grep "Window start:" docs/DOGFOOD.md` shows a YYYY-MM-DD date
    - `grep "Binary:" docs/DOGFOOD.md` shows a git SHA (7+ hex chars)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "fill in" docs/DOGFOOD.md || echo "0 placeholders remaining"</automated>
  </verify>
  <done>DOGFOOD.md header has concrete window start date and binary commit SHA; no `<!-- fill in ... -->` placeholders remain.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 2: complete ≥7-day dogfood window and sign off</name>
  <what-built>
    - DOGFOOD.md initialized with window start date and binary SHA
    - Binary wired in Claude Code CLI via .mcp.json (from 07-03)
    - README Agent Setup docs complete (from 07-02)
  </what-built>
  <how-to-verify>
    Over the next ≥7 days of active use:

    1. **Daily logging:** Add one entry per day to the `## Log` section of `docs/DOGFOOD.md` using the Day N template. Record which agent you used, what calls you made, any issues observed.

    2. **Observe structured-error types:** Try to trigger (or observe naturally):
       - A `not_found` result — e.g., ask for a non-existent package version
       - A `rate_limited` result — e.g., make many GitHub Actions lookups without a GITHUB_TOKEN
       - An `upstream_down` result — or note if none occurred during the window
       Log these in the Observations table.

    3. **P0 bug check:** If you hit any of these P0 conditions during use, open them in the P0 tracker table:
       - stdout corruption (JSON-RPC framing broken)
       - Wrong latest-stable returned (e.g., prerelease surfaced as stable)
       - Scoped npm package failure (e.g., `@types/node` fails)
       - `+incompatible` suffix dropped for Go modules
       - Maven group-path encoding bug

    4. **Sign off:** Once ≥7 days have elapsed and the P0 tracker is empty, check all four boxes in the `## Sign-off` section of DOGFOOD.md:
       - [x] ≥7 days elapsed
       - [x] Zero open P0 bugs
       - [x] Rate-limit, not-found, and upstream-down events observed in stderr logs at least once each
       - [x] Author approves v1.0.0 tag

    5. Commit the completed DOGFOOD.md.

    **Minimum bar for sign-off:**
    - At least 7 dated log entries in `## Log`
    - P0 tracker shows no open items (all rows resolved or "— none open")
    - All four sign-off checkboxes checked
  </how-to-verify>
  <resume-signal>
    Type "signed off" once DOGFOOD.md is complete and all sign-off boxes are checked. Or describe any blocking P0 bugs that need fixing before tagging v1.0.0.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → DOGFOOD.md | Author-only log file; no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Repudiation | dogfood sign-off | accept | Author-only repo; sign-off is a manual acknowledgement; git history provides audit trail |
</threat_model>

<verification>
- `grep -c "fill in" docs/DOGFOOD.md` returns 0
- `grep -c "\[x\]" docs/DOGFOOD.md` returns 4 (all sign-off boxes checked)
- `grep -c "### Day" docs/DOGFOOD.md` returns ≥7
- P0 tracker table contains no open items
</verification>

<success_criteria>
Dogfood window complete: ≥7 daily log entries in DOGFOOD.md, zero open P0 bugs, all sign-off checkboxes checked, DOGFOOD.md committed. Ready to tag v1.0.0.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dogfooding-v1-0-0/07-04-SUMMARY.md`
</output>
