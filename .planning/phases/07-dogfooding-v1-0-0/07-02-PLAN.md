---
phase: 07-dogfooding-v1-0-0
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - README.md
autonomous: true
requirements:
  - VALD-01
  - VALD-02

must_haves:
  truths:
    - "README contains ## Agent Setup section with subsections for all 5 agents"
    - "Each agent section has a verified JSON/TOML config block with the correct format"
    - "GITHUB_TOKEN callout appears in each agent section"
    - "Codex CLI and Gemini CLI sections carry an explicit unverified-by-author note"
    - "Warn against committing token values appears in the GITHUB_TOKEN guidance"
  artifacts:
    - path: "README.md"
      provides: "Multi-agent setup docs for all 5 agents"
      contains: "## Agent Setup"
  key_links:
    - from: "README.md"
      to: "## Agent Setup"
      via: "new top-level section replacing / merging old Claude Desktop config section"
      pattern: "## Agent Setup"
---

<objective>
Write the full `## Agent Setup` section in README.md covering all five AI coding agents. This is the net-new documentation work of Phase 7 — providing every agent's config format, GITHUB_TOKEN env var guidance, and clear verification-status labels for unverified configs.

Purpose: Any user (or the author) should be able to read README.md and wire the binary into their agent of choice without hunting for config format docs.
Output: README.md updated with a complete, accurate Agent Setup section.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-dogfooding-v1-0-0/07-CONTEXT.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>task 1: write ## Agent Setup section in README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (read entire file to understand current structure and find the existing "Claude Desktop configuration" section to fold in)
    - .planning/phases/07-dogfooding-v1-0-0/07-CONTEXT.md (sections B and D contain the exact config formats and GITHUB_TOKEN examples — copy verbatim)
  </read_first>
  <action>
    Add a new top-level `## Agent Setup` section to README.md. Per CONTEXT.md §A, this goes in-place in README (no docs/ subdirectory). Fold the existing "Claude Desktop configuration" section into the new structure.

    The section must contain these subsections in order, with the exact config formats from CONTEXT.md §B and GITHUB_TOKEN examples from CONTEXT.md §D:

    ---

    **`## Agent Setup`** — opening paragraph: "The server speaks MCP over stdio. Every agent that supports stdio MCP servers can use it. Add the binary path after downloading and (on macOS) clearing quarantine."

    **`### Claude Desktop`**
    - Install via MCPB bundle (one-click, recommended) — reference the MCPB section already in README
    - Manual config at `~/Library/Application Support/Claude/claude_desktop_config.json` (macOS):
      ```json
      {
        "mcpServers": {
          "version-check-mcp": {
            "command": "/path/to/version-check-mcp"
          }
        }
      }
      ```
    - GITHUB_TOKEN callout (see below for format)
    - Verification status: **Verified**

    **`### Claude Code CLI`**
    - Project-scoped config at `.mcp.json` in project root:
      ```json
      {
        "mcpServers": {
          "version-check-mcp": {
            "command": "/path/to/version-check-mcp",
            "type": "stdio"
          }
        }
      }
      ```
    - Global alternative: `~/.claude/claude.json` (same schema)
    - GITHUB_TOKEN callout with env field:
      ```json
      {
        "mcpServers": {
          "version-check-mcp": {
            "command": "/path/to/version-check-mcp",
            "type": "stdio",
            "env": { "GITHUB_TOKEN": "ghp_..." }
          }
        }
      }
      ```
    - Verification status: **Verified** (confirmed from `.mcp.json` in this repo)

    **`### OpenCode`**
    - Config at `opencode.json` or `opencode.jsonc` at project root, or `~/.config/opencode/opencode.json` globally:
      ```json
      {
        "mcp": {
          "version-check-mcp": {
            "type": "local",
            "command": ["/path/to/version-check-mcp"]
          }
        }
      }
      ```
    - GITHUB_TOKEN callout:
      ```json
      "version-check-mcp": {
        "type": "local",
        "command": ["/path/to/version-check-mcp"],
        "env": { "GITHUB_TOKEN": "ghp_..." }
      }
      ```
    - Verification status: **Verified** (confirmed from author's `~/.config/opencode/opencode.jsonc`)

    **`### Codex CLI`**
    - Config at `~/.codex/config.toml` (global) or `.codex/config.toml` (project-scoped, trusted projects only):
      ```toml
      [mcp_servers.version-check-mcp]
      command = "/path/to/version-check-mcp"
      ```
    - CLI shortcut: `codex mcp add version-check-mcp -- /path/to/version-check-mcp`
    - GITHUB_TOKEN callout:
      ```toml
      [mcp_servers.version-check-mcp.env]
      GITHUB_TOKEN = "ghp_..."
      ```
    - Verification status: **Based on official Codex CLI docs — not personally tested by the author.**

    **`### Gemini CLI`**
    - Config at `~/.gemini/settings.json`:
      ```json
      {
        "mcpServers": {
          "version-check-mcp": {
            "command": "/path/to/version-check-mcp"
          }
        }
      }
      ```
    - CLI shortcut: `gemini mcp add version-check-mcp /path/to/version-check-mcp`
    - GITHUB_TOKEN callout:
      ```json
      "env": { "GITHUB_TOKEN": "$GITHUB_TOKEN" }
      ```
      Note: Gemini CLI supports shell variable expansion (`$VAR`). Other agents set literal values.
    - Verification status: **Based on official Gemini CLI docs — not personally tested by the author.**

    **GITHUB_TOKEN warning** (appears once, inline or as a shared note before the callout blocks):
    > **Warning:** Do not commit token values to `.mcp.json`, `opencode.json`, or any version-controlled config file. Use a secrets manager or environment-level injection instead.

    Remove (or merge) the old standalone "Claude Desktop configuration" section if it exists, so there is no duplicate.
  </action>
  <acceptance_criteria>
    - `grep -c "## Agent Setup" README.md` returns 1
    - `grep -c "### Claude Desktop" README.md` returns 1
    - `grep -c "### Claude Code CLI" README.md` returns 1
    - `grep -c "### OpenCode" README.md` returns 1
    - `grep -c "### Codex CLI" README.md` returns 1
    - `grep -c "### Gemini CLI" README.md` returns 1
    - `grep -c "GITHUB_TOKEN" README.md` returns ≥5 (one per agent section)
    - `grep -c "not personally tested" README.md` returns ≥2 (Codex and Gemini)
    - `grep -c "Do not commit" README.md` returns ≥1
    - `grep -c "LimitReader\|Claude Desktop configuration" README.md` on the old standalone section returns 0 (old section removed or merged)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "## Agent Setup" README.md && grep -c "### Claude Code CLI" README.md && grep -c "GITHUB_TOKEN" README.md && grep -c "not personally tested" README.md</automated>
  </verify>
  <done>README.md contains a complete ## Agent Setup section with all 5 agent subsections, correct config formats matching CONTEXT.md §B verbatim, GITHUB_TOKEN guidance in each section per CONTEXT.md §D, unverified notes on Codex and Gemini, and a warning against committing tokens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author → README | Documentation only; no code execution, no external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | GITHUB_TOKEN in config examples | mitigate | Add explicit warning: "Do not commit token values to version-controlled config files" per CONTEXT.md §D |
</threat_model>

<verification>
- `grep -c "## Agent Setup" README.md` == 1
- `grep -c "### Claude Code CLI" README.md` == 1
- `grep -c "### Gemini CLI" README.md` == 1
- `grep -c "GITHUB_TOKEN" README.md` >= 5
- `grep -c "not personally tested" README.md` >= 2
- `grep -c "Do not commit" README.md` >= 1
</verification>

<success_criteria>
README.md Agent Setup section is complete and accurate: all 5 agents documented with correct config formats, GITHUB_TOKEN guidance inline, and unverified configs clearly labelled.
</success_criteria>

<output>
After completion, create `.planning/phases/07-dogfooding-v1-0-0/07-02-SUMMARY.md`
</output>
